<% if @message.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@message.errors.count, "error") %> prohibited this repa from being saved:</h2>

    <ul>
    <% @message.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<div class="container">
<h1>Envoi d'un message à <%= User.find(params[:user_id]).prenom %> <%= User.find(params[:user_id]).nom %></h1>

<%= form_for(@message) do |f| %>
  <%= f.hidden_field :dest_id, :value => params[:user_id] %>
  <%= f.text_area :message %>

  <%= f.submit "Envoyer" %>
<% end %>
</div>
